export default class Service {
    getFibEndpoint: string = '';
    getFibResultEndpoonit: string = '';

    getFibData = (number) => {
        // http call
        console.log(number);
    };

    getFibResults = () => {
        // http call
    };
}